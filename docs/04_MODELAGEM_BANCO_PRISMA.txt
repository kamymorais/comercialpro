# COMERCIALPRO — MODELAGEM DO BANCO / PRISMA

======================================================================
1. VISÃO GERAL
======================================================================

Banco: PostgreSQL no Neon.
ORM: Prisma.

A modelagem precisa atender:

- Cadastro pendente.
- Aprovação pelo admin.
- Perfis hierárquicos.
- Consultor vinculado a gerente.
- Previsão diária por consultor.
- Edição por gerente/regional/admin.
- Reset diário sem apagar histórico.
- Sessão com expiração de 3 horas.

======================================================================
2. ENUMS
======================================================================

enum Role {
  ADMIN
  REGIONAL_MANAGER
  MANAGER
  CONSULTANT
}

enum UserStatus {
  PENDING
  APPROVED
  REJECTED
}

enum RequestedRole {
  REGIONAL_MANAGER
  MANAGER
  CONSULTANT
}

enum ResetStatus {
  SUCCESS
  SKIPPED
  ERROR
}

======================================================================
3. MODEL USER
======================================================================

User representa qualquer pessoa do sistema.

Campos recomendados:

- id
- fullName
- username
- passwordHash
- requestedRole
- role
- status
- managerId
- regionalManagerId
- approvedById
- approvedAt
- rejectedAt
- createdAt
- updatedAt

Regras:

- username deve ser único.
- passwordHash nunca deve ser enviado para o frontend.
- role pode ser nulo enquanto o usuário estiver pendente.
- requestedRole registra o que a pessoa pediu no cadastro.
- status inicia como PENDING.
- managerId é usado para consultor.
- regionalManagerId pode ser usado para vincular gerente a regional no futuro.

======================================================================
4. MODEL DAILY FORECAST
======================================================================

DailyForecast representa a previsão diária de um consultor.

Campos recomendados:

- id
- consultantId
- operationalDate
- productionValue
- insuranceValue
- tcValue
- noForecast
- submittedAt
- updatedById
- createdAt
- updatedAt

Regras:

- Deve existir no máximo uma previsão por consultor por data operacional.
- Criar unique composto:
  - consultantId + operationalDate
- Valores monetários devem usar Decimal.
- Se noForecast = true, os valores devem ser tratados como zero.
- updatedById registra quem fez a última edição.

======================================================================
5. MODEL SESSION
======================================================================

Session representa a sessão de login.

Campos recomendados:

- id
- userId
- tokenHash
- expiresAt
- createdAt
- revokedAt

Regras:

- Sessão expira após 3 horas.
- tokenHash é salvo no banco.
- Cookie guarda o token bruto.
- Nunca salvar token bruto em log.
- Se expiresAt < now, sessão inválida.

======================================================================
6. MODEL RESET LOG
======================================================================

ResetLog registra execução do reset diário.

Campos recomendados:

- id
- operationalDate
- executedAt
- status
- message

Regras:

- operationalDate deve ser único.
- Se já existir ResetLog SUCCESS para a data, não executar reset novamente.
- O reset deve ser idempotente.

======================================================================
7. SCHEMA PRISMA SUGERIDO
======================================================================

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

enum Role {
  ADMIN
  REGIONAL_MANAGER
  MANAGER
  CONSULTANT
}

enum UserStatus {
  PENDING
  APPROVED
  REJECTED
}

enum RequestedRole {
  REGIONAL_MANAGER
  MANAGER
  CONSULTANT
}

enum ResetStatus {
  SUCCESS
  SKIPPED
  ERROR
}

model User {
  id                 String        @id @default(cuid())
  fullName           String
  username           String        @unique
  passwordHash       String
  requestedRole      RequestedRole
  role               Role?
  status             UserStatus    @default(PENDING)

  managerId          String?
  manager            User?         @relation("ManagerConsultants", fields: [managerId], references: [id])
  consultants        User[]        @relation("ManagerConsultants")

  regionalManagerId  String?
  regionalManager    User?         @relation("RegionalManagers", fields: [regionalManagerId], references: [id])
  managers           User[]        @relation("RegionalManagers")

  approvedById       String?
  approvedBy         User?         @relation("ApprovedUsers", fields: [approvedById], references: [id])
  approvedUsers      User[]        @relation("ApprovedUsers")

  approvedAt         DateTime?
  rejectedAt         DateTime?

  sessions           Session[]
  forecasts          DailyForecast[] @relation("ConsultantForecasts")
  updatedForecasts   DailyForecast[] @relation("ForecastUpdatedBy")

  createdAt          DateTime      @default(now())
  updatedAt          DateTime      @updatedAt

  @@index([status])
  @@index([role])
  @@index([managerId])
  @@index([regionalManagerId])
}

model DailyForecast {
  id               String    @id @default(cuid())
  consultantId     String
  consultant       User      @relation("ConsultantForecasts", fields: [consultantId], references: [id])

  operationalDate  DateTime

  productionValue  Decimal   @default(0) @db.Decimal(12, 2)
  insuranceValue   Decimal   @default(0) @db.Decimal(12, 2)
  tcValue          Decimal   @default(0) @db.Decimal(12, 2)

  noForecast       Boolean   @default(false)
  submittedAt      DateTime?

  updatedById      String?
  updatedBy        User?     @relation("ForecastUpdatedBy", fields: [updatedById], references: [id])

  createdAt        DateTime  @default(now())
  updatedAt        DateTime  @updatedAt

  @@unique([consultantId, operationalDate])
  @@index([operationalDate])
  @@index([consultantId])
}

model Session {
  id          String    @id @default(cuid())
  userId      String
  user        User      @relation(fields: [userId], references: [id])

  tokenHash   String    @unique
  expiresAt   DateTime
  revokedAt   DateTime?

  createdAt   DateTime  @default(now())

  @@index([userId])
  @@index([expiresAt])
}

model ResetLog {
  id               String      @id @default(cuid())
  operationalDate  DateTime    @unique
  executedAt       DateTime    @default(now())
  status           ResetStatus
  message          String?

  createdAt        DateTime    @default(now())
}

======================================================================
8. REGRA DE DATA OPERACIONAL
======================================================================

O sistema zera às 19h.

Existem duas formas:

FORMA SIMPLES:
- Usar a data calendário do Brasil.
- Às 19h, cron marca reset para o dia.
- A partir do reset, novas previsões são da próxima data.

FORMA RECOMENDADA:
- Criar função getOperationalDate().
- Se horário atual for antes de 19h, operationalDate = data atual.
- Se horário atual for igual ou depois de 19h, operationalDate = próxima data.

Exemplo:
- 08/07/2026 18:30 -> operationalDate = 08/07/2026
- 08/07/2026 19:01 -> operationalDate = 09/07/2026

Isso faz o sistema "zerar" sem apagar dados.

======================================================================
9. CONSULTAS IMPORTANTES
======================================================================

Total do consultor:
- Buscar DailyForecast do consultantId na operationalDate atual.

Total do gerente:
- Buscar consultores com managerId = gerente.id.
- Buscar previsões da operationalDate atual.
- Somar productionValue + insuranceValue + tcValue.

Total do regional:
- Buscar gerentes.
- Buscar consultores dos gerentes.
- Buscar previsões da operationalDate atual.
- Somar valores.

Total admin:
- Buscar todos os consultores aprovados.
- Buscar previsões da operationalDate atual.
- Somar valores.

======================================================================
10. OBSERVAÇÃO SOBRE HISTÓRICO
======================================================================

Não deletar DailyForecast no reset.

O reset visual acontece porque a aplicação muda a data operacional.

Esse modelo permite histórico futuro sem retrabalho.

======================================================================
11. MODELAGEM DO DIARIO DE VISITA
======================================================================

Migration criada:
- prisma/migrations/20260729162000_add_visit_diary/migration.sql

Novos enums:
- VisitStatus
- VisitEventType

Novos models:
- VisitAssignment
- VisitEvent

Relacoes adicionadas em User:
- assignedVisits
- managedVisits
- visitEvents

Indices adicionados:
- VisitAssignment(consultantId, status)
- VisitAssignment(managerId, status)
- VisitAssignment(createdAt)
- VisitEvent(visitAssignmentId, createdAt)
- VisitEvent(authorId)

Detalhamento completo em docs/modulo-diario-visita/03_MODELAGEM.txt.

Atualizacao de fotos do Diario de visita:
- Migration prisma/migrations/20260729174500_add_visit_photos/migration.sql.
- Model VisitPhoto vinculado a VisitEvent.
- Relacao User.uploadedPhotos.
- VisitEvent.photos.
